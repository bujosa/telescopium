import request from "supertest";

it("Has a route handler listening to /api/tickets fro post request", () => {});

it("Can only be accessed if the user is signed in", () => {});

it("return an error if an invalid title is provided", () => {});

it("return an error if an invalid price is provided", () => {});

it("creates a tickets with valid inputs", () => {});
